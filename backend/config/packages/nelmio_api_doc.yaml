nelmio_api_doc:
    documentation:
        info:
            title: VetCare API
            description: API de gestion pour la Clinique Vétérinaire des Étangs.
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
